# Изменения бэкенда — что знать фронтенд-команде

> Актуально на 2026-06-12. Бэкенд завершён: реализованы **все четыре подсистемы**
> (управление заявками, события, приоритет + автоматическая маршрутизация, аналитика),
> плюс релизный режим с резервным копированием, чат заявки и дублирование
> уведомлений на рабочую почту. **Фронтенд мы не трогали.**
> Существующий контракт не сломан — 294 интеграционных теста зелёные; актуальная
> спецификация API — `docs/openapi.yaml`.
>
> Ниже: (1) главное изменение поведения, (2) новые эндпоинты и поля,
> (3) список задач на фронте по убыванию важности.

## 1. Главное: бэкенд теперь меняет данные сам, в фоне

Раз в 30 секунд фоновый цикл:

- **пересчитывает приоритет** всех открытых заявок (фактор срока растёт со временем —
  заявка может «дозреть» до Высокого/Критичного без действий пользователя);
- **автоматически назначает** заявки в статусе «Новый» свободным подходящим
  исполнителям (по отделу, матрице грейдов, занятости и кулдауну отдела) — статус
  становится `assigned` без участия руководителя;
- **критичная заявка** распределяется немедленно при создании; если свободных нет —
  она **вытесняет** наименее приоритетную не-критичную заявку у подходящего
  исполнителя (та возвращается в «Новый» с признаком «Незавершённая»);
- помечает **просроченные** заявки и шлёт уведомления (см. ниже).

Следствия для фронта:

- ✅ **опрос по таймеру уже реализован** (`usePolling`, лёгкий список + карточка
  выбранной заявки + уведомления; интервал `VITE_POLL_INTERVAL_MS`, по умолчанию 15с) —
  фоновые изменения бэкенда доезжают до открытой страницы автоматически;
- у заявки в статусе `new` может быть заполнен `executorId`/`executor` — это след
  прежнего исполнителя после возврата/вытеснения (поле `previousExecutorId` тоже
  заполнено). Не считайте `executorId != null` признаком «назначена» — смотрите на
  `status`;
- уведомлений стало заметно больше, и они приходят без действий пользователя:
  - «Вам назначена заявка …» (авто-назначение);
  - «Заявка … снята с вас …» (вытеснение критичной / переназначение);
  - «Критичная заявка … вытеснила …», «… не удалось распределить автоматически»
    (руководителю; в т.ч. для обычных заявок без свободных исполнителей);
  - «истекает срок» / «заявка просрочена» (руководителю **и исполнителю**, по всем
    открытым статусам);
- каждое уведомление теперь дополнительно **дублируется на рабочую почту**
  сотрудника (если включено в конфиге бэкенда). Для фронта это прозрачно: API,
  лента уведомлений и бейдж не изменились, никакой работы не требуется.
  Подробности — `apps/backend/docs/email-notifications.md`.

## 2. Новые эндпоинты и поля

### Аналитика — 4 эндпоинта, контракт полный
`GET /analytics/applications | /executors | /work-types | /departments`
(право `canViewReports`; обычный руководитель — свой отдел, top-manager — все;
фильтры `createdFrom`/`createdTo`, отсутствие = за всё время).

> 📄 Полный контракт полей — `docs/analytics-contract.md`. Реализован **весь** набор
> метрик, включая `idleTimeSeconds`/`occupancyRatio` (время простоя и доля занятости
> исполнителей/отделов — раньше были `null`, теперь считаются). Формат стабилен,
> страницы аналитики можно верстать; финальное согласование формата — за вами.

### Настройки отдела: `PATCH /departments/{id}`
`{ employeeApplicationDelayMinutes?, deadlineNotificationRatio? }` (хотя бы одно поле),
`204` при успехе. Обычный руководитель — только свой отдел (`403`), top-manager — любой;
`deadlineNotificationRatio` валидируется в `[0..1]` (`422`). UI для этого ещё нет — см.
задачи ниже.

### `GET`/`PUT /priority-settings` → read-only блок `urgent`
```jsonc
{
  "department": { ... }, "managerAuthor": { ... }, "deadline": 1.0,
  "urgent": { "thresholdHours": 24, "bonus": 0.5 }   // read-only, для предпросмотра
}
```
`urgent` теперь приходит и в ответе **PUT** (тип `PrioritySettings` на фронте един для
GET и PUT — повторный GET после сохранения больше не обязателен).

### Чат заявки (новые эндпоинты)
Переписка автор ↔ исполнитель ↔ руководитель в карточке заявки (polling, не WebSocket):
- `GET /applications/{id}/messages?afterId=` → `{ items, unreadCount }` (читать может
  любой, кто видит карточку; `afterId` — инкрементальная дозагрузка);
- `POST /applications/{id}/messages` `{text}` → `201` (пишут автор/исполнитель/
  руководитель-в-scope; закрытая заявка → `409`);
- `POST /applications/{id}/messages/read` → `204` (маркер прочтения).
Новое сообщение создаёт уведомление собеседнику в общую ленту (с анти-спамом).
Заявка ID 4 в сиде уже содержит демо-чат. Полное ТЗ —
**`docs/frontend-task-chat.md`** (новая задача 3.10).

### Почтовые уведомления (без изменений API — только к сведению)
Все внутрисистемные уведомления (назначение, вытеснение, дедлайны, эскалации,
сообщения чата) дублируются письмом на рабочую почту сотрудника. Два режима
адресации: `login_domain` (логин + домен организации, напр. `orlova_m@kptws.ru`)
или `ad` (атрибут `mail` из Active Directory). По умолчанию выключено
(`email.enabled=false` в конфиге бэкенда). **Фронтенду делать ничего не нужно** —
ни новых эндпоинтов, ни полей; настройки почты в UI не выводятся.

### Должности у видов работ: `allowedPositionIds` (новая ось допуска)
**Принцип назначения изменён**: авто-распределение теперь проверяет ДВЕ оси — должность
сотрудника ∈ `workType.allowedPositionIds` И грейд ∈ `workType.allowedGradeIds`.
- `GET /work-types` и вложенный `workType` карточки: новое поле
  `allowedPositionIds: string[]` (**пустой список = любая должность** — все
  существующие виды работ работают как раньше).
- `POST/PATCH /work-types`: необязательное `allowedPositionIds` (id из
  `GET /positions`); в PATCH список заменяется целиком, `[]` снимает ограничение.
- Ручное назначение руководителем по-прежнему матрицы не проверяет.

Полное ТЗ для фронта — **`docs/frontend-task-worktype-positions.md`** (задача 3.6).

### Должности: `positions[].gradeIds`
`GET /positions` отдаёт у каждой должности её возможные грейды из матрицы `post_grade`:
```jsonc
{ "items": [
  { "id": "1", "name": "Инженер",         "gradeIds": ["0", "1"] },  // junior, middle
  { "id": "3", "name": "Руководитель",    "gradeIds": ["2", "3"] }   // senior, lead
] }
```
После появления `allowedPositionIds` это поле нужно в основном для UX-подсказки:
если у выбранных должностей и выбранных грейдов нет пересечения, авто-распределение
не найдёт ни одного кандидата (см. ТЗ выше).

### Вложения: `attachments[].contentType`
В карточке заявки у вложений появился MIME-тип (например `image/png`). Маппер фронта
уже читает это поле — изображения начнут классифицироваться как «фото» автоматически,
менять ничего не нужно.

### Excel-отчёт — честный xlsx
`GET /reports/applications.xls` (путь сохранён) теперь отдаёт
`Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` и
`Content-Disposition: attachment; filename=applications.xlsx`.

### Ужесточения доступа (фронт уже ведёт себя правильно — ничего делать не нужно)
- `GET /ad/users` теперь требует `canManageEmployees` (остальным `403`). Фронт и так
  запрашивает его только при этом праве.
- Карточка чужой заявки (`GET /applications/{id}`) недоступна непричастным — `404`
  (видимость как у списка).
- Загрузка вложений — только автор / назначенный исполнитель / руководитель в scope
  (`403` остальным).
- Руководитель не может выдать сотруднику роль выше своей
  (`PATCH /employees/{id}` `role=top-manager` от обычного руководителя → `403`).
- Смена роли сотрудника через `PATCH /employees/{id}` теперь **действует сразу**
  (раньше — только после перезапуска бэкенда).

## 3. Задачи на фронте

> Статус на 2026-06-12 (после сверки с фронт-коммитами): выполнено всё, кроме 3.6.

### 3.1. ✅ Опрос по таймеру — выполнено
По ТЗ `frontend-task-live-updates.md`: `usePolling`, лёгкий список + карточка выбранной
заявки + уведомления, интервал `VITE_POLL_INTERVAL_MS`.

### 3.2. ✅ Пагинация списка — выполнено
`pageSize=100` + `applicationsTotal`/`hasMoreApplications` по `pagination.total`.

### 3.3. ✅ Предпросмотр приоритета — выполнено
Плавный `deadlinePressure` от реального `Date.now()`, бонус срочности из
`GET /priority-settings → urgent` — формула совпадает с бэкендом.

### 3.4. ✅ Диапазон коэффициента отдела до 1.25 — выполнено

### 3.5. ✅ Настройки отдела — выполнено
`updateDepartmentSettings` → `PATCH /departments/{id}` со страницы приоритетов.

### 3.6. Должности у видов работ (ЕДИНСТВЕННАЯ оставшаяся)
В форме вида работ добавить мультиселект должностей (`allowedPositionIds`) и показывать
их в списке/карточке («Любая должность» для пустого списка). Авто-распределение на
бэкенде уже назначает по должности+грейду. Полное ТЗ —
**`docs/frontend-task-worktype-positions.md`**.
⚠️ Текущий блок «Должности: …» на WorkTypesPage (`getAllowedPositionNames`) вычисляет
должности **эвристикой по грейдам** — после введения оси `allowedPositionIds` он может
показывать неверный список (например, для видов работ, ограниченных конкретной
должностью). Заменить на данные из `workType.allowedPositionIds` (см. ТЗ).

### 3.7. ✅ Занятость исполнителя при назначении — выполнено
Предупреждение в окне назначения о возврате прежней заявки в «Новый».

### 3.8. ✅ Имя файла отчёта — выполнено
Имя берётся из `Content-Disposition` (фоллбек `applications-report.xlsx`).

### 3.9. ✅ Страницы аналитики — выполнено
DTO и вызовы всех четырёх `/analytics/*` совпадают с `docs/analytics-contract.md`.

### 3.10. Чат заявки (новое)
Блок чата в карточке заявки: список сообщений + ввод, опрос через `usePolling`,
право писать — как у бэка (автор/исполнитель/руководитель-в-scope), read-only для
закрытых заявок. Полное ТЗ — **`docs/frontend-task-chat.md`**.

### По желанию
- Бейдж «Просрочена»: `application.is_expired` бэкенд проставляет, но в API карточки
  пока не отдаёт. Скажите — добавим поле в `GET /applications/{id}` (неломающее).
- В `shared/model/applicationRules.ts` остались мёртвые локальные копии
  `canViewApplication`/`getAvailableApplicationActions` — `availableActions` и
  видимость давно считает бэкенд; копии можно удалить, чтобы не разъезжались.

## 4. Эксплуатация (фронту полезно знать)

- **Прод** разворачивается через `docker-compose.prod.yml` (Caddy, HTTPS): фронт —
  `https://<APP_DOMAIN>`, API — `https://<API_DOMAIN>`, S3 — `https://<S3_DOMAIN>`
  (домены в `.env`).
- **Релизный режим** (`SEED_ON_START=false`): база больше не пересевается демо-данными
  при рестарте; при выключении бэкенд снимает резервную копию БД в S3. В dev-режиме
  (по умолчанию) пересев на каждый старт сохраняется — демо-логины/данные те же.
- Контракт API не зависит от режима; `docs/openapi.yaml` всегда можно перегенерировать
  из живого бэкенда (`GET /openapi.json`).
